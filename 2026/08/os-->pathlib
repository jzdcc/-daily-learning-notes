import os
os是用于让python代码去操作系统 不同于人为在cmd中操作
os.getcwd()                获得当前工作路径 vscode以当前根目录为所有的工作路径

path1="D:/test"
os.path.exists(path1)       判断路径下的目录是否存在
os.listdir(path1)            遍历路径下的文件文件夹 但不会递归
os.walka(path)               返回的是由路径，文件夹列表，文件列表组成的元组

path=os.getcwd()+"/文件名"
os.mkdir(path)                创建单层文件夹 若已存在就会报错
path2=os.getcwd()+"/文件名/文件名2"
os.mkdirs()                    创建一个递归的文件夹
os.rmdir()                      只能删除空文件夹
os.path.join(path,filename)     自动适配电脑系统进行路径拼接
os.path.split(path)              输出路径和文件名两个部分 以元组方式返回
os.path.dirname(path)            只返回路径
os.path.basename(path)                只返回文件名
os.path.getsize(path)            如果具体到文件 返回文件大小  文件夹初始会给一个块的大小：4096
os.path.abspath(path)            取决于os.getcwd() 返回path的绝对路径 absolute绝对的
os.environ                       读取环境变量 os.environ[PATH]PATH这个环境变量的内容 输入不认识的代码 系统会依次根据这个路径查找
os.system()                      执行系统命令 相当于把括号内容放在cmd上输出
pathlib 简介明了 从左到右的阅读顺序更符合直觉

                from pathlib import Path
Path.iterdir()          
for p in  Path().iterdir()
print(p)                Path()不传参数默认工作目录 单层遍历工作目录
Path().rglob("*vscode*",case_sensitive=False)  深层遍及工作目录的文件 并查找无论大小写带有vsccde的文件  

p=Path(file.txt)
print(p.name)            file.txt  
print(p.stem)            file
print(p.suffix)          .txt
print(p.exists())         True False
print(p.parent)            输出父目录 可以链式叠加 当前工作目录输出为 . （-->priject
print(p.parent.absolute())  输出当前工作目录即.的绝对路径
new_file=new_file=my_dir/"new_file.txt"
print(new_file)            获得新文件路径
case_sensitive=False       不区分大小写
p=Path.("TempFile.txt")
p.touch                    创建文件夹
p.unlink                   删除带内容的文件

p.rmdir()                  删除空文件夹

import shutil
shutil.rmtree(文件夹名)              删含子文件 有内容的文件夹

p=Path("TempDir/Sundir")
p.mkdir(parents=True)       创建了一个有子文件的目录
