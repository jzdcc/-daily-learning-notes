安装了vmware 配合cenos虚拟硬件构建了属于自己的linux虚拟机
            命令本体+选项+参数                             通配符*
            命令本体就是一个个的二进制可执行程序
            管道符 | ；将符号左边的结果作为右边的输入
ls -a \                         显示\（根目录下）所有的文件包括隐藏文件 .a开头的
ls -l                           以列表形式展现当前home目录的文件内容
ls -lh                          在列表形式的前提下 以更易方便阅读的形式（内存大小后加上单位）表示
cd                              更改当前所在目录
pwd                             查看当前所在的工作目录
.                               表示当前目录 例如cd ./Desktop == cd /home/wyl/Desktop == cd Desktop 绝对路径以根目录为起点 相对路径以当前目录为起点 
..                              返回上一级目录   cd ../..表示切换到上两级目录
~                               表示HOME目录 cd ~/Desktop 表示切换到HOME目录中的Desktop目录
mkdir (-p) +路径/文件名          用来创建文件夹
mkdir -p ~/itcast/wyl/nice/666  用来创建多个层级的目录
toch test.txt                   用来创建文件
cat +路径                       直接显示文件的所有内容
more +路径                      文件内容过多时翻页查看内容 空格下一页 q退出
cp 1 2                          将文件1复制到文件2 如果是文件夹的复制需要加上-r
mv 1 2                          移动1到2中 文件文件夹都可以 如果2不存在则将1进行改名
rm -r -f                        -a表示删除文件夹 -f表示强制删除（不再进行确认） 可以一次性删除多个
which cd &which pwd& which cp   查看命令的程序文件在哪里
find +起始路径 -name ""   find + 起始路径 -size +或-k.M.G                          根据名字 大小查找文件
grep -n                         （-n可选，表示在结果中显示匹配的行的行号）从文件中通过关键字过滤文件行
wc -l -w -c -m                   统计文件的行数 单词数 字节数 字符数
echo                             在命令行输出指定内容
` `                              被反引号包围的内容会被作为命令执行而非普通字符 例如 echo `pwd` 输出结果不是pwd
>> >                             重定向符>表示 覆盖 写入右侧文件   >>追加写入
tail -f -n                       查看文件尾部内容 -f可以持续最终 -n例如-5表示查看后五行内容 没有时默认为10行
用vim在命令行模式下编辑文件 命令模式 i进入输入模式 esc回到命令模式 ：进入运行模式 wq进行保存和退出
