双指针指的是一个循环 一个指针一直走；ing一个指针（slow）选择性前进

88 ---合并排列数组
给两个数组 整合成一个并且按从小到大顺序
（注意 "非递减顺序就是指的从小到大顺序")
/直接合并后用.sort（）
//双指针 相互嵌套 O（m=n）

27 ---移除数组内元素
给一个数组和一个值 将数组内与值相等的除去
（直接覆盖相等的值
/双指针

283---移动0
在不改变原数组有效数字"相对顺序"的前提下将所含的0放在数组最后
/slow=0
 for 循环 如果nums[fast]!=0 nums[slow]=nums[fast] solw递增
最后将nums[slow]后的全部变为0

125---回文
判断字符串（不考虑非字母和数字的）全部改为小写后 从前到后和从后到前是否一致
/isalnum()用来保留是数字或字母的 lower()用来改为小写-upper可以用来全部改为大写 +while的从前到后 从后到前的两个循环 如果此刻都是字母数字字符还需判断小写后是否相同
