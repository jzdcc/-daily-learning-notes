本质字典
1 --- two Sum
给一个目标值和列表 求让列表中哪两个的和为目标值（返回下标
/暴力解法 双循环
//哈希解法+enumerate（列表
好久没写了 前后差不多弄了一个小时 
217 --- contains Duplicate
给一个列表 若其中的有重复值返回true
/将列表转换为集合set（nums） 看前后长度是否有变化
//将列表值全部放入哈希表中 然后循环+if
242 --- valid Anagram
给两个字符串 判断是否为anagram
  先判断长度
/使用内置哈希工具Counter（自动遍历字符串 统计每个元素出现次数）直接return Counter(s)=Counter(t)
//用累加确定s的hushmap 再累减t的字符 最终循环判断累减后是否全部为0
383 --- Ransom Note
给两个字符串 判断第一个能否由第二个构成
/使用Counters + for递减 等于0就False
